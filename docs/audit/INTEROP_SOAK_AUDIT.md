# Interop- & Soak-Audit — Fehlerregister

> Lebendes Register. **Nur Dokumentation — kein autonomes Fixen.** Jeder Fix ist ein separates,
> eigens freigegebenes Paket. Design: `docs/audit/2026-07-21-interop-soak-audit-design.md`.

**Finding-Typen:** `Interop-Abweichung` · `Soak-Leak` · `Media-Defekt` · `Wire-Robustheit` · `Facade-Coupling-Gap`

| FID | Typ | Evidenz (Test/Peer) | Symptom | Fehlerquelle | Fundstelle (Datei:Zeile) | Fix-Vorschlag | Schweregrad | Status |
|-----|-----|---------------------|---------|--------------|--------------------------|---------------|-------------|--------|
| F001 | Facade-Coupling-Gap | Phase 0.1 (InteropHarness-Setup) | L0–L3-Komponenten (`RtpCallMediaSession` u. a.) sind `internal`; Test unter der Facade erfordert `InternalsVisibleTo`-Eintrag | Bewusste Kapselung: Sub-Facade-Typen nicht öffentlich | `src/Core/Infrastructure/Rtp/RtpCallMediaSession.cs:22`, `src/Core/Properties/AssemblyInfo.cs:12` (hinzugefügter InteropHarness-Eintrag) | Kein Fix — dokumentiert. Bewertung, ob ein schmales öffentliches Test-/Diagnose-Seam sinnvoll ist, in späterem Paket | Info | dokumentiert |
| F002 | Media-Defekt | Phase 2.3 (`MediaQualityDriftSoakTests`, 20 s Loopback) | `PacketsUnrecoverableLoss=17..50` auf reinem UDP-Loopback ohne echten Netzwerkverlust; `DroppedLate=219`, Delivered=761 bei ~1000 gesendeten Paketen | `RtpCallMediaSession.EmitConcealmentFramesIfNeeded` behandelt Sequenzlücken nach Late-Drops als „unrecoverable loss": `_lastDeliveredSequence` wird bei Late-Drops **nicht** vorgerückt; nächstes regulär geliefertes Paket erzeugt Gap = Anzahl der Late-Drops; Gap > `MaxConcealmentBurstPackets` (3) → `AddUnrecoverableLoss(gap-3)`. Late-Drops entstehen, weil `Task.Delay(20ms)` auf einem .NET-GC/OS-Scheduler Burst-Arrhythmien erzeugt, die den Jitter-Buffer-Playout-Deadline überschreiten. | `src/Core/Infrastructure/Rtp/RtpCallMediaSession.cs:540-558` (`EmitConcealmentFramesIfNeeded`) · `src/Core/Infrastructure/Rtp/JitterBuffer/JitterBuffer.cs:144-149` (`Late`-Pfad) | Optionen: (A) `_lastDeliveredSequence` bei `Late`-Drop-Pfad mitführen, sodass kein falscher Gap zum nächsten Paket entsteht; (B) Concealment-Gap nicht aus `_lastDeliveredSequence` ableiten, sondern aus der tatsächlich gelieferten Paketreihe; (C) Loopback-Soak mit adaptiertem Playout-Delay / größerem Initial-Delay testen, um .NET-Scheduler-Bursts zu absorbieren. Echte Netzwerkszenarien profitieren von Option A. | Niedrig–Mittel (nur lokale QoS-Delivery-Metrik verfälscht; RTCP-Wire-Report korrekt; keine Datenpfad-Korruption) | offen — adversarial verifiziert (opus), Präzisierung unten |
| F003 | Facade-Coupling-Gap | Phase 3 (Signaling-Soak-Recon) | Langzeit-Signaling ist nur real-time-beschleunigt testbar (kurzer Expires), NICHT echt zeitgerafft — `SipLineChannel`-Refresh-Loop und `SipSessionTimerManager` nutzen hart `Task.Delay`, keine `ITimeProvider`/Clock-Abstraktion | Fehlende Zeit-Abstraktion im Signaling-Layer → 100+-Zyklen-Soaks bräuchten reale Stunden | `src/Core/Infrastructure/Sip/Adapters/SipLineChannel.cs` (RegisterAsync-Loop, `Task.Delay`) · `src/Core/Infrastructure/Sip/Signaling/SessionTimers/SipSessionTimerManager.cs` (`RunScheduleAsync`, `Task.Delay`) | Kein Fix — dokumentiert; optionaler `ITimeProvider`-Seam (~60 Z., 2 Injektionspunkte) würde echtes Zeit-Rafting ermöglichen, separates Paket | Info (Testbarkeit) | dokumentiert |
| F004 | Facade-Coupling-Gap | Phase 5 P1d (`MediaQualityDriftSoakTests.RoundTripTime_IsStaticHint…`, 15 s Loopback) | `RoundTripTimeMs` ist über den gesamten Lauf konstant — der Wert bleibt der statische Anlauf-Hint, keine Live-RTCP-SR/RR-Messung | Der bare L2-`RtpCallMediaSession` instanziiert/verdrahtet KEINEN RTCP-Quality-Monitor; die RTT-Messung (RFC 3550 §6.4.1, SR/RR/DLSR) wird erst vom L3-`CallMediaOrchestrator` via `CallRtcpQualityMonitor` aufgesetzt. `EstimatedRoundTripTimeMs` wird nur mit dem ~60-ms-Hint geseedet und ohne Monitor nie aktualisiert. | `src/Core/Infrastructure/Rtp/RtpCallMediaSession.cs` (`EstimatedRoundTripTimeMs`, Hint-Seed) · `src/Core/Application/Media/CallMediaOrchestrator.cs` (verdrahtet `CallRtcpQualityMonitor`) · `src/Core/Application/Media/CallRtcpQualityMonitor.cs:467-514` (RTT-Berechnung) | Kein Fix — dokumentiert. RTT ist eine L3-Orchestrator-Fähigkeit; eine echte RTT-Messung im Soak erfordert einen L3/L4-Harness (oder das Einziehen von `CallRtcpQualityMonitor` in die Fixture). RTT-Assertions gehören auf L3+, nicht in den L2-Media-Loopback. | Info (Schicht-Grenze) | dokumentiert |
| F005 | Facade-Defekt | Phase 6 (`AsteriskRegisterFailureInteropTests`, echter Asterisk) | Bei falschem Passwort / unbekanntem User erreicht die Line intern `LineState.Failed` (Auth permanent abgelehnt), aber die Convenience-`ConnectAsync` **short-circuittet nicht**: sie wartet das volle `ConnectOptions.Timeout` ab (8 s bei 8-s-Timeout gemessen) und meldet dann `ConnectStatus.Timeout` mit **`Error == null`**. `FinalLineState == Failed` ist im Ergebnis vorhanden — der Status widerspricht ihm. | Die Register-Warteschleife pollt nur auf `Registered` bis Timeout und wertet den terminalen `LineState.Failed` NICHT als Abbruchbedingung; der intern vorhandene `SipRegistrationFailedException` (401/403) wird nicht in `ConnectResult.Error` durchgereicht. | `src/Core/Application/Convenience/SdkConvenienceOrchestrator.cs` (RegisterAndWait-Warteschleife) · `src/Client/Application/Workflows/ConnectResult.cs` (Status/Error-Mapping) | In der Warteschleife terminalen `LineState.Failed` als Abbruch behandeln → sofort `ConnectStatus.Failed`. **FIX (PR #44, `a65db79`):** `ShouldCompleteConnectWait` wertet terminales `LineState.Failed` als Abbruch → `ConnectStatus.Failed` short-circuittet statt das volle Timeout abzuwarten (verifiziert `WrongPassword_ReportsFailedStatusPromptly`, <15 s statt 20 s). **Rest F005b:** Auth-Fehler noch nicht als `ConnectResult.Error` durchgereicht. | Niedrig–Mittel (permanente Auth-Ablehnung nicht von transientem Timeout unterscheidbar) | **GEFIXT (PR #44)** — Status/Short-Circuit; Rest F005b offen |
| F006 | SIP-Defekt (RFC 3261 §12.1.2) — **GEFIXT** | Phase 6 (`DialAndWaitUntilConnectedAsync` gegen echten Asterisk; via SDK-`LoggerFactory`-Wire-Trace + Code-Verifikation) | **Authentifizierte ausgehende Calls scheiterten grundsätzlich.** Auth-Retry-INVITE (CSeq 2 nach 401-Challenge) → **`481 Call/Transaction Does Not Exist`**, uniform über alle Ziele, unabhängig von Transport/SRTP. Differenzierer: REGISTER-Auth-Retry ging, INVITE-Auth-Retry nicht. (Nebenbefund: der anfangs beobachtete `Connection refused` war die UDP-only-Fixture vs. RFC-§18.1.1-TCP-Eskalation des 1305-B-SRTP-INVITE — SDK korrekt, nicht die Ursache.) | **Ursache:** `SipCallSessionTransactionService.cs:223` übernahm den **To-Tag der 401-Fehlerantwort** in `_context.RemoteTag`; `SipCallSessionHeaderService.cs:61-63` hängte ihn an den `To` des Retry-INVITE → syntaktisch in-dialog für einen nie etablierten Dialog → strikter UAS (Asterisk) antwortet 481. **RFC 3261 §12.1.2:** eine Nicht-2xx-Antwort etabliert KEINEN Dialog; ihr To-Tag darf nicht adoptiert werden. Der Dialog-Tag wird korrekt nur via `ApplyInviteDialogResponse` (`_dialogManager.ConfirmedRemoteTag`, nur 2xx / reliable-1xx) bestätigt — Zeile 223 überschrieb das. REGISTER immun (`SipRegistrationService.cs:126` baut To einmalig). | `src/Core/Infrastructure/Sip/Signaling/Dialogs/SipCallSessionTransactionService.cs:223` (entfernte RemoteTag-Kontamination) · `.../SipCallSessionHeaderService.cs:61-63` | **FIX (Branch `fix/invite-auth-retry-totag`):** die Nicht-2xx-To-Tag-Adoption (Zeile 223) entfernt — RemoteTag wird nur noch dialog-bestätigt gesetzt; ACK für Nicht-2xx bleibt unberührt (auto-gesendet vom Client-Transaction-Layer mit dem Response-To-Tag). Regressionstest `SipInviteAuthRetryToTagTests` (Retry-INVITE-To ist tag-los). **Verifiziert:** +1 Test grün, 410 SIP-Integrationstests grün; **End-to-End gegen echten Asterisk: 481 verschwunden**, ausgehende Calls erreichen den Dialplan (busy→486, nonexistent→404 mit Plain-RTP; SRTP-Angebot→488 media-mismatch = Fixture-Config, kein Bug). | **Hoch** (blockierte alle authentifizierten ausgehenden Calls) | **gefixt** — entsperrt Gruppe B (Call-Non-Happy-Path als Follow-up) |
| F007 | Security-Härtung — **GEFIXT (Warnung+Doku)** | Phase 6 (SRTP-Analyse beim Group-B-488; Code-Verifikation) | Im Default (`SrtpPolicy.Optional`, `OfferDtlsSrtp=false`, `DefaultTransport=Udp`) bietet das SDK SRTP per **SDES** an — der Master-Key reist als `a=crypto`-Zeile **im Klartext-SDP** über ungesicherte Signalisierung. Verschlüsselung wirkt aktiv (SAVP-Profil, fail-closed bei `Required`, Anti-Bidding-Down — alles korrekt), ist aber gegen einen **passiven Mitleser auf dem Signalisierungspfad wirkungslos**, weil er den Key mitliest. „Immer SRTP" verspricht mehr als der Default liefert. | **RFC 4568 §7:** SDES setzt eine vertrauliche Signalisierung (TLS/SIPS) voraus; das SDK erzwang/warnte das nicht. `SipCoreCallChannel.cs:188` bietet SDES bei `policy != Disabled && !DTLS` unabhängig vom Transport an. DTLS-SRTP (RFC 5763, Keys nie im SDP) wäre sicher, ist aber `OfferDtlsSrtp=false` per Default und für die meisten SIP-Peers (Asterisk/Fritzbox: nur SDES) nicht nutzbar. | `src/Core/Infrastructure/Sip/Adapters/SipCoreCallChannel.cs:188` (SDES-Offer) · `src/Client/Domain/Configuration/VoipConfiguration.cs` (Default Optional/`OfferDtlsSrtp=false`) | **FIX (Branch `security/sdes-insecure-signaling-warning`), nicht-brechend:** `SrtpPolicyEvaluator.ExposesSdesKeyOverInsecureSignaling(policy, offerDtls, signalingSecure)` + Warn-once im `SipLineChannel` (ausgehend) mit RFC-4568-§7-Hinweis + Reason-Code `SdesKeyOverInsecureSignaling` + Doku-Klarstellung an `VoipConfiguration.SrtpPolicy`. **Bewusstes Nicht-tun:** hart-verweigern oder DTLS-Default = Breaking Change (bricht SIP-Interop mit SDES-only-Peers) → **opt-in-Enforce** (`RequireSecureSignalingForSdes`) als Follow-up. +Matrix-Unit-Test `SrtpPolicyEvaluatorSdesExposureTests`. **Enforce umgesetzt (PR #45):** `SipLineChannel.GuardSdesKeyExposure` verweigert bei gesetztem Flag fail-closed (`SipLineChannelSdesEnforcementTests`). | Mittel (Security-DX: over-promised-Default; real nur ohne TLS-Signalisierung ausnutzbar; SDES-über-TLS und DTLS unbetroffen; Verhandlungs-Mechanik selbst korrekt) | **GEFIXT** — Warnung+Doku (PR #40) + opt-in-Enforce (PR #45) |
| F008 | Facade-Defekt | Phase 6 Gruppe B (`AsteriskCallFailureInteropTests.NoAnswer…`, echter Asterisk) | Ausgehender Call an ein **ringendes** Ziel (no-answer): `DialWaitOptions.ConnectTimeout=4s` wird NICHT geehrt — der Call wartet ~30 s (Ring-/Transaktions-Timeout) und meldet dann `DialStatus.Failed` (synthetischer 408) statt `DialStatus.Timeout` innerhalb ConnectTimeout. | Die Convenience-`DialAndWaitUntilConnectedAsync`-Warteschleife bricht bei einem 1xx-ringenden Call nicht bei `ConnectTimeout` ab; sie läuft in den unteren Ring-/INVITE-Transaktions-Timeout (synthetischer 408) und mappt das auf `Failed` statt `Timeout`. | `src/Core/Application/Convenience/SdkConvenienceOrchestrator.cs` (Dial-Warteschleife / ConnectTimeout) · `src/Client/Application/Workflows/DialResult.cs` (Status-Mapping) | `ConnectTimeout` auch für ringende (1xx) Calls als harte Obergrenze durchsetzen → bei Ablauf CANCEL + `DialStatus.Timeout`. **FIX (PR #44, `a65db79`):** `timeoutCts`/`linkedCts` umschließen den gesamten Dial → `DialStatus.Timeout` bei ConnectTimeout-Ablauf. | Mittel (Call hing bis RingTimeout statt ConnectTimeout; Timeout→Failed fehlgemappt) | **GEFIXT (PR #44)** — verifiziert `NoAnswer_ShouldTimeoutWithinConnectTimeout` |
| F009 | Facade-Defekt | Phase 6 Gruppe B (`AsteriskCallFailureInteropTests.CancelledDial…`, echter Asterisk) | Externe Cancellation (`CancellationToken`) während des Wählens: der Call bricht zeitgerecht ab (~1 s), aber das Ergebnis ist `DialStatus.Failed` (synthetischer 408) statt **`DialStatus.Canceled`**. | Beim Cancel wird die INVITE-Transaktion terminiert (synthetischer 408); die Convenience-Ergebnis-Bildung prüft nicht, ob der Abbruch durch die Caller-Cancellation kam, und mappt auf `Failed` statt `Canceled`. `DialWaitOptions.HangupOnCancellation` (default true) sendet zwar CANCEL, aber der Status ist falsch. | `src/Core/Application/Convenience/SdkConvenienceOrchestrator.cs` (Cancel-Behandlung in der Dial-Warteschleife) · `src/Client/Application/Workflows/DialResult.cs` | Bei ausgelöster Caller-Cancellation `DialStatus.Canceled` zurückgeben (vor dem generischen Failed-Mapping prüfen), analog zum `ConnectResult.Canceled`-Pfad. **FIX (PR #44, `a65db79`):** `catch when (ct.IsCancellationRequested)` → `DialStatus.Canceled` vor dem Failed-Mapping. | Niedrig–Mittel (funktional/DX: gewollter Abbruch nicht von echtem Fehler unterscheidbar) | **GEFIXT (PR #44)** — verifiziert `CancelledDial_ShouldYieldCanceled` |
| F005b | Facade-Defekt | Phase 6 Follow-up (`WrongPassword_ShouldExposeAuthError`, echter Asterisk) | Bei permanenter Auth-Ablehnung ist `ConnectResult.Status` korrekt `Failed` (F005 gefixt), aber `ConnectResult.Error` bleibt `null` — der zugrunde liegende Auth-Fehler (401/403, `SipRegistrationFailedException`) wird nicht durchgereicht. | Der F005-Fix wertet terminales `LineState.Failed` als Abbruch, transportiert aber die Fehlerursache der Line nicht in `ConnectResult.Error`. | `src/Core/Application/Convenience/SdkConvenienceOrchestrator.cs` (Connect-Warteschleife) · `src/Client/Application/Workflows/ConnectResult.cs` (Error-Mapping) | Beim terminalen `Failed` die Line-Fehlerursache (Auth-Exception) in `ConnectResult.Error` durchreichen. **FIX:** `SipLineChannel` publiziert die permanente Failure-Reason (`LineReconnectFailed`) jetzt VOR dem State-Übergang; der Convenience-Orchestrator erfasst sie und mappt sie beim terminalen `Failed` auf `ConnectResult.Error` (`RegistrationFailureError`). Verifiziert: `WrongPassword_ShouldExposeAuthError` grün, 33 Convenience/Register-Unit-Tests regressionsfrei. | Niedrig (DX: Status korrekt, aber keine maschinenlesbare Fehlerursache) | **GEFIXT** — Auth-Fehler als ConnectResult.Error sichtbar |
| F010 | Interop-Abweichung (RFC 5626 / NAT) | Phase 6+ (`AsteriskTcp-/TlsTransportInteropTests`, echter Asterisk über **TCP und TLS** + Docker-Bridge-NAT; SDK-Wire-Trace) | Erste TCP-/TLS-Registration erhält **200 OK** (bei TLS inkl. erfolgreichem Handshake); danach schreibt der NAT-korrektive Re-Register den Contact auf die vom Registrar reflektierte SNAT-Adresse (`172.17.0.1:<port>`) um und re-registriert → Asterisk **403 Forbidden** → Line Failed. **TCP und TLS identisch betroffen**; über UDP stabil (grüne Register-Tests). | Der NAT-korrektive Re-Register (rport/received-basiert) wird **transport-unabhängig** angewendet. Über verbindungsorientierte Transporte (TCP/TLS) ist das Umschreiben des Contact auf die gesehene Source falsch: die persistente Verbindung übernimmt das Routing (RFC 5626 SIP Outbound), der ephemere SNAT-Port taugt nicht als stabiler Contact. `ReregisterOptions.MaxCorrectiveReregistrations` ist nicht öffentlich konfigurierbar → kein Workaround. | `src/Core/Infrastructure/Sip/Adapters/SipLineChannel.cs:415-444` (`NatPublicContactState.ApplyObserved` + corrective re-register) | Den NAT-korrektiven Re-Register auf verbindungslose Transporte (UDP) beschränken; über TCP/TLS die bestehende Verbindung wiederverwenden und den Contact nicht umschreiben. **FIX:** `SipLineChannel` wendet den NAT-korrektiven Re-Register nur noch bei `SipTransport.Udp` an (`natCorrectionApplies`); über TCP/TLS bleibt die erste Registration bestehen (RFC 5626). Verifiziert: `AsteriskTcp-/TlsTransportInteropTests` grün (Register + Call), 75 Register/NAT-Unit-Tests regressionsfrei. | Mittel (blockiert TCP-Registration hinter NAT; ohne NAT unbetroffen — dort keine Contact-Diskrepanz) | **GEFIXT** — TCP+TLS-Register/Call grün |

| F011 | Interop-Abweichung (Early Media) | Phase 6+ (`AsteriskEarlyMediaInteropTests`, echter Asterisk; SDK-Wire-Trace + Code-Verifikation) | Der SDK empfängt **183 Session Progress**, setzt aber **keine Media-Session aus dessen SDP** auf: es gibt keinen Pre-Answer-RTP-Empfang (Early Media). `MediaParametersNegotiated` feuert erst beim 200 OK. Zusätzlich kehrt `DialAsync` erst nach der INVITE-Transaktion zurück → während des Ringing existiert kein Call-Handle zur Beobachtung. | Der `OnProvisionalResponse`-Handler verarbeitet nur Dialog-Tag, reliable-PRACK (100rel) und die Ringing-Transition; er wertet die 183-SDP nicht aus und startet keine Empfangs-Media-Session. | `src/Core/Infrastructure/Sip/Signaling/Dialogs/SipCallSessionTransactionService.cs:138-169` (`OnProvisionalResponse`) · `src/Core/Domain/Lines/PhoneLine.cs:67-95` (`DialAsync` kehrt nach der Transaktion zurück) | Bei 183+SDP eine receive-only-Media-Session bis zum 200 OK aufsetzen und ein Call-Handle für die Pre-Answer-Beobachtung exponieren. Größeres Feature, kein autonomer Fix. | Niedrig–Mittel (Netz-Ansagen/Ringback vor dem Verbinden gehen verloren; der Rufaufbau selbst ist unbetroffen) | offen — verifiziert (Code + Wire-Trace) |

## Coverage-Notiz Media-Qualität (P1d, 2026-07-21)

Media-Drift-Soak ist von „nur Jitter/PCMU/Plain" auf die **volle Matrix** erweitert: **Codec {PCMU, Opus} × Sicherheit {Plain RTP, SRTP-SDES}** (4 Kombinationen, je `SoakShort`+`SoakLong`). Opus läuft transport-only (opake Payload, PT 111/48 kHz/960 Samples); SRTP über SDES-Keying (`AES_CM_128_HMAC_SHA1_80`, richtungsweise getauschte Master-Keys). Geprüft je Kombination: `PacketsDelivered>0`, **`PacketsDroppedOverflow==0`** (eigenständige Verlustursache, getrennt von Late/Loss), Jitter-Nicht-Drift (Long). `PacketsUnrecoverableLoss==0` bleibt **F002-blockiert** (Skip). RTT/RTCP: siehe **F004** (an L2 nicht messbar).

## Coverage-Notiz Signaling (P1e, 2026-07-21)

Der frühere „Signaling-Soak" ist präzise als **`SipLineChannelRefreshLifecycleSoakTests`** benannt (Scope-Ehrlichkeit). Er prüft ausschließlich den Re-REGISTER-**Refresh-Scheduler** des echten SipLineChannel gegen einen **In-Memory-Fake-Registrar** (`RecordingRegistrationService`): Zyklus-Kadenz, monotone CSeq, stabile Call-ID — plus eine **Ressourcenreihe** (nebenläufiges Sampling → Managed-Regression + Threads/Sockets/FDs-Plateau, nur Long). **Kein Nachweis** von Wire-Format, Transaktions-Layer, Digest-Auth, Transport oder Interop — echter SIP-Wire-Soak wäre ein eigener L4-Interop-Lauf (vgl. **F003**: fehlende Zeit-Abstraktion verhindert echtes Zeit-Rafting).

## Coverage-Notiz Asterisk-Interop-Matrix (Phase 6+, 2026-07-22)

Die Asterisk-Interop-Suite (`tests/CalloraVoipSdk.InteropTests`, echter `andrius/asterisk:22` via Testcontainers) deckt jetzt die volle SIP/RTP-Matrix ab — **22 grün, 4 Skip** (befund-blockiert):

**Grün:** Register-Happy + Failure (Wrong-PW/Unknown-User → Failed prompt [F005], Unreachable → Timeout); Call-Failure (busy/486, decline/403, unknown/404); no-answer-Timeout [F008], Cancel→Canceled [F009]; **Happy-Path Outbound** (INVITE→200→ACK→RTP→BYE); **Codec-Negotiation** (PCMU/PCMA/G722 per Präferenz); **SRTP-SDES** (RTP/SAVP + `a=crypto`, verschlüsseltes Media); **DTMF** RFC 4733 (Empfang + Verhandlung); **Hold/Unhold** (re-INVITE); **Inbound Call** (Asterisk→SDK via `channel originate`); **Blind + Attended Transfer** (REFER/Replaces); **Session-Timer**-Verhandlung (RFC 4028).

**Skip (befund-blockiert):** TCP- + TLS-Register [**F010**: NAT-Re-Register transport-unabhängig → 403]; Early Media [**F011**: kein 183-Media-Setup]; Auth-Error-Durchreichung [**F005b**].

Media-Assertion unidirektional (`SilenceAudioDevice` sendet nichts): Asterisk sendet Milliwatt/Töne, Empfang via `ICall.RtpStatistics.PacketsReceived`. Plain RTP außer im SDES-Test. Neue Befunde dieser Phase: **F005b, F010, F011**.

## Verifikations-Notiz F002 (adversarial, opus, 2026-07-21)

Kausalkette Ende-zu-Ende gegen den echten Code bestätigt (Verdict **CONFIRMED**):

- **Doppel-Zählung:** Ein Late-Paket IST angekommen (RFC 3550: „received") und wird trotzdem als `PacketsUnrecoverableLoss` gezählt — zusätzlich zu `PacketsDroppedLate`. Auf reinem Loopback (0 echter Verlust) ist jedes `PacketsUnrecoverableLoss>0` definitionsgemäß falsch.
- **Impact eingegrenzt:** betrifft NUR die lokale QoS-Delivery-Metrik `PacketsUnrecoverableLoss`. Der RTCP-Wire-Report-Pfad (`InboundRtpStatistics.TrackSequence`/`CaptureRtcpReport`, `InboundRtpStatistics.cs:75-149`) ist separat und zählt jedes empfangene Paket → on-wire `CumulativePacketsLost` bleibt korrekt.
- **Präzise Fundstellen:** `EmitConcealmentFramesIfNeeded` @538–559 (Gap = `incomingSeq-(_lastDeliveredSequence+1)` @543f; `AddUnrecoverableLoss(gap-count)` @557f) · `MaxConcealmentBurstPackets`=3 (`const` @28) · `_lastDeliveredSequence` (`ushort` @70, vorgerückt nur bei Delivery @533 + Concealment @553) · `JitterBuffer` Late-Pfad @145–150 (return VOR Insert, kein Cursor) · Session-Handler `HandleJitterBufferAddResult` Late-Branch @660 (nur `RecordDroppedLate()`+Log — **hier fehlt das Cursor-Vorrücken**).
- **Fix-Präzisierung:** gehört in den Session-`Late`-Handler @660; `_lastDeliveredSequence` extended-sequence-aware auf `max(...)` vorrücken (analog telephone-event-Pfad @391f) UND Late gar nicht zu `UnrecoverableLoss` beitragen lassen. **Warnung: naives Vorrücken bei jedem Late maskiert echten Out-of-Order-Loss** → extended-seq-Vergleich zwingend.
- **Alternativen ausgeschlossen:** `PacketsDroppedOverflow=0` schließt Jitter-Buffer-Overflow aus; das Symptom skaliert mit dem `Late`-Count, nicht mit Socket-Loss.
