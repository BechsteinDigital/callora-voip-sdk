# Interop- & Soak-Audit — Fehlerregister

> Lebendes Register. **Nur Dokumentation — kein autonomes Fixen.** Jeder Fix ist ein separates,
> eigens freigegebenes Paket. Design: `docs/audit/2026-07-21-interop-soak-audit-design.md`.

**Finding-Typen:** `Interop-Abweichung` · `Soak-Leak` · `Media-Defekt` · `Wire-Robustheit` · `Facade-Coupling-Gap`

| FID | Typ | Evidenz (Test/Peer) | Symptom | Fehlerquelle | Fundstelle (Datei:Zeile) | Fix-Vorschlag | Schweregrad | Status |
|-----|-----|---------------------|---------|--------------|--------------------------|---------------|-------------|--------|
| F001 | Facade-Coupling-Gap | Phase 0.1 (InteropHarness-Setup) | L0–L3-Komponenten (`RtpCallMediaSession` u. a.) sind `internal`; Test unter der Facade erfordert `InternalsVisibleTo`-Eintrag | Bewusste Kapselung: Sub-Facade-Typen nicht öffentlich | `src/Core/Infrastructure/Rtp/RtpCallMediaSession.cs:22`, `src/Core/Properties/AssemblyInfo.cs:12` (hinzugefügter InteropHarness-Eintrag) | Kein Fix — dokumentiert. Bewertung, ob ein schmales öffentliches Test-/Diagnose-Seam sinnvoll ist, in späterem Paket | Info | dokumentiert |
| F002 | Media-Defekt | Phase 2.3 (`MediaQualityDriftSoakTests`, 20 s Loopback) | `PacketsUnrecoverableLoss=17..50` auf reinem UDP-Loopback ohne echten Netzwerkverlust; `DroppedLate=219`, Delivered=761 bei ~1000 gesendeten Paketen | `RtpCallMediaSession.EmitConcealmentFramesIfNeeded` behandelt Sequenzlücken nach Late-Drops als „unrecoverable loss": `_lastDeliveredSequence` wird bei Late-Drops **nicht** vorgerückt; nächstes regulär geliefertes Paket erzeugt Gap = Anzahl der Late-Drops; Gap > `MaxConcealmentBurstPackets` (3) → `AddUnrecoverableLoss(gap-3)`. Late-Drops entstehen, weil `Task.Delay(20ms)` auf einem .NET-GC/OS-Scheduler Burst-Arrhythmien erzeugt, die den Jitter-Buffer-Playout-Deadline überschreiten. | `src/Core/Infrastructure/Rtp/RtpCallMediaSession.cs:540-558` (`EmitConcealmentFramesIfNeeded`) · `src/Core/Infrastructure/Rtp/JitterBuffer/JitterBuffer.cs:144-149` (`Late`-Pfad) | Optionen: (A) `_lastDeliveredSequence` bei `Late`-Drop-Pfad mitführen, sodass kein falscher Gap zum nächsten Paket entsteht; (B) Concealment-Gap nicht aus `_lastDeliveredSequence` ableiten, sondern aus der tatsächlich gelieferten Paketreihe; (C) Loopback-Soak mit adaptiertem Playout-Delay / größerem Initial-Delay testen, um .NET-Scheduler-Bursts zu absorbieren. Echte Netzwerkszenarien profitieren von Option A. | Niedrig–Mittel (nur lokale QoS-Delivery-Metrik verfälscht; RTCP-Wire-Report korrekt; keine Datenpfad-Korruption) | offen — adversarial verifiziert (opus), Präzisierung unten |
| F003 | Facade-Coupling-Gap | Phase 3 (Signaling-Soak-Recon) | Langzeit-Signaling ist nur real-time-beschleunigt testbar (kurzer Expires), NICHT echt zeitgerafft — `SipLineChannel`-Refresh-Loop und `SipSessionTimerManager` nutzen hart `Task.Delay`, keine `ITimeProvider`/Clock-Abstraktion | Fehlende Zeit-Abstraktion im Signaling-Layer → 100+-Zyklen-Soaks bräuchten reale Stunden | `src/Core/Infrastructure/Sip/Adapters/SipLineChannel.cs` (RegisterAsync-Loop, `Task.Delay`) · `src/Core/Infrastructure/Sip/Signaling/SessionTimers/SipSessionTimerManager.cs` (`RunScheduleAsync`, `Task.Delay`) | Kein Fix — dokumentiert; optionaler `ITimeProvider`-Seam (~60 Z., 2 Injektionspunkte) würde echtes Zeit-Rafting ermöglichen, separates Paket | Info (Testbarkeit) | dokumentiert |
| F004 | Facade-Coupling-Gap | Phase 5 P1d (`MediaQualityDriftSoakTests.RoundTripTime_IsStaticHint…`, 15 s Loopback) | `RoundTripTimeMs` ist über den gesamten Lauf konstant — der Wert bleibt der statische Anlauf-Hint, keine Live-RTCP-SR/RR-Messung | Der bare L2-`RtpCallMediaSession` instanziiert/verdrahtet KEINEN RTCP-Quality-Monitor; die RTT-Messung (RFC 3550 §6.4.1, SR/RR/DLSR) wird erst vom L3-`CallMediaOrchestrator` via `CallRtcpQualityMonitor` aufgesetzt. `EstimatedRoundTripTimeMs` wird nur mit dem ~60-ms-Hint geseedet und ohne Monitor nie aktualisiert. | `src/Core/Infrastructure/Rtp/RtpCallMediaSession.cs` (`EstimatedRoundTripTimeMs`, Hint-Seed) · `src/Core/Application/Media/CallMediaOrchestrator.cs` (verdrahtet `CallRtcpQualityMonitor`) · `src/Core/Application/Media/CallRtcpQualityMonitor.cs:467-514` (RTT-Berechnung) | Kein Fix — dokumentiert. RTT ist eine L3-Orchestrator-Fähigkeit; eine echte RTT-Messung im Soak erfordert einen L3/L4-Harness (oder das Einziehen von `CallRtcpQualityMonitor` in die Fixture). RTT-Assertions gehören auf L3+, nicht in den L2-Media-Loopback. | Info (Schicht-Grenze) | dokumentiert |
| F005 | Facade-Defekt | Phase 6 (`AsteriskRegisterFailureInteropTests`, echter Asterisk) | Bei falschem Passwort / unbekanntem User erreicht die Line intern `LineState.Failed` (Auth permanent abgelehnt), aber die Convenience-`ConnectAsync` **short-circuittet nicht**: sie wartet das volle `ConnectOptions.Timeout` ab (8 s bei 8-s-Timeout gemessen) und meldet dann `ConnectStatus.Timeout` mit **`Error == null`**. `FinalLineState == Failed` ist im Ergebnis vorhanden — der Status widerspricht ihm. | Die Register-Warteschleife pollt nur auf `Registered` bis Timeout und wertet den terminalen `LineState.Failed` NICHT als Abbruchbedingung; der intern vorhandene `SipRegistrationFailedException` (401/403) wird nicht in `ConnectResult.Error` durchgereicht. | `src/Core/Application/Convenience/SdkConvenienceOrchestrator.cs` (RegisterAndWait-Warteschleife) · `src/Client/Application/Workflows/ConnectResult.cs` (Status/Error-Mapping) | In der Warteschleife terminalen `LineState.Failed` als Abbruch behandeln → sofort `ConnectStatus.Failed` mit dem Auth-Fehler statt Timeout+null (Short-Circuit statt volles Timeout zu verbrauchen). Kein autonomer Fix. | Niedrig–Mittel (permanente Auth-Ablehnung nicht von transientem Timeout unterscheidbar; volle Timeout-Zeit verschwendet; keine Fehlerinfo) | offen — verifiziert (Phase 6, echter Asterisk) |
| F006 | Interop-Abweichung — in Untersuchung | Phase 6 (Versuch Gruppe B, `DialAndWaitUntilConnectedAsync` gegen echten Asterisk; via SDK-`LoggerFactory` gemessen) | **Authentifizierte ausgehende Calls an Asterisk scheitern grundsätzlich.** Ablauf: INVITE (CSeq 1) → **401** Auth-Challenge → ACK → Auth-Retry-INVITE (CSeq 2) → **`481 Call/Transaction Does Not Exist`** (`SipFinalResponseException`, `DialStatus.Failed`, ~1 s). **Uniform** über alle Ziele (busy/decline/nonexistent/noanswer) und **unabhängig von Transport (UDP/TCP) und SRTP (Optional/Disabled)** → der Call erreicht nie den Dialplan, 486/603/404/no-answer/CANCEL bleiben untestbar. **★ Differenzierer: REGISTER-Auth-Retry funktioniert (401→200), nur der INVITE-Auth-Retry bekommt 481.** | Root-Cause-Kandidat (durch den Differenzierer gestützt, noch zu bestätigen): das SDK echot den **To-Tag aus der 401-Challenge in den Auth-Retry-INVITE** → Asterisk hält ihn für in-dialog → 481 (RFC 3261 §8.1.3.5/§12.2: der re-INVITE nach 401 ist dialog-bildend, To OHNE Tag). Alternativ Asterisk-pjsip-Dialog-Matching-Quirk. **Nebenbefund (SDK korrekt, nicht Ursache):** mit Default-SRTP-Angebot ist der INVITE 1305 B > 1300-MTU → das SDK eskaliert RFC 3261 §18.1.1-konform auf TCP; eine UDP-only-Fixture liefert dann `Connection refused`. Mit TCP-Transport ODER `SrtpPolicy.Disabled` (kleiner UDP-INVITE) tritt das darunterliegende 481 zutage. | Auth-Retry-Pfad des ausgehenden INVITE in `src/Core/Infrastructure/Sip/Signaling/Dialogs`/`Ingress` (To-Header des CSeq-2-INVITE prüfen) · `src/Core/Infrastructure/Sip/Transport/SipTransportRuntime.cs:181-215` (MTU→TCP-Eskalation, korrekt) | (1) To-Header des Auth-Retry-INVITE erfassen (volles Wire-Logging) → To-Tag-Echo bestätigen/widerlegen. (2) Gegen zweiten Peer (FreeSWITCH/Fritzbox) prüfen → SDK-Defekt vs. Asterisk-spezifisch klassifizieren. Reproduktion: pjsip.conf um `[transport-tcp]` erweitern (oder `SrtpPolicy.Disabled`) + Dialplan `[default]` (busy→Busy(), decline→Hangup(21), noanswer→Ringing()+Wait). Kein Fix vor Ursache. | **Potenziell hoch** (blockiert authentifizierte ausgehende Calls an Asterisk = Kern-Use-Case; ganze Call-Interop-Spur blockiert) — final abhängig von SDK-vs-Asterisk-Klassifizierung | offen — in Untersuchung (Gruppe B blockiert) |

## Coverage-Notiz Media-Qualität (P1d, 2026-07-21)

Media-Drift-Soak ist von „nur Jitter/PCMU/Plain" auf die **volle Matrix** erweitert: **Codec {PCMU, Opus} × Sicherheit {Plain RTP, SRTP-SDES}** (4 Kombinationen, je `SoakShort`+`SoakLong`). Opus läuft transport-only (opake Payload, PT 111/48 kHz/960 Samples); SRTP über SDES-Keying (`AES_CM_128_HMAC_SHA1_80`, richtungsweise getauschte Master-Keys). Geprüft je Kombination: `PacketsDelivered>0`, **`PacketsDroppedOverflow==0`** (eigenständige Verlustursache, getrennt von Late/Loss), Jitter-Nicht-Drift (Long). `PacketsUnrecoverableLoss==0` bleibt **F002-blockiert** (Skip). RTT/RTCP: siehe **F004** (an L2 nicht messbar).

## Coverage-Notiz Signaling (P1e, 2026-07-21)

Der frühere „Signaling-Soak" ist präzise als **`SipLineChannelRefreshLifecycleSoakTests`** benannt (Scope-Ehrlichkeit). Er prüft ausschließlich den Re-REGISTER-**Refresh-Scheduler** des echten SipLineChannel gegen einen **In-Memory-Fake-Registrar** (`RecordingRegistrationService`): Zyklus-Kadenz, monotone CSeq, stabile Call-ID — plus eine **Ressourcenreihe** (nebenläufiges Sampling → Managed-Regression + Threads/Sockets/FDs-Plateau, nur Long). **Kein Nachweis** von Wire-Format, Transaktions-Layer, Digest-Auth, Transport oder Interop — echter SIP-Wire-Soak wäre ein eigener L4-Interop-Lauf (vgl. **F003**: fehlende Zeit-Abstraktion verhindert echtes Zeit-Rafting).

## Verifikations-Notiz F002 (adversarial, opus, 2026-07-21)

Kausalkette Ende-zu-Ende gegen den echten Code bestätigt (Verdict **CONFIRMED**):

- **Doppel-Zählung:** Ein Late-Paket IST angekommen (RFC 3550: „received") und wird trotzdem als `PacketsUnrecoverableLoss` gezählt — zusätzlich zu `PacketsDroppedLate`. Auf reinem Loopback (0 echter Verlust) ist jedes `PacketsUnrecoverableLoss>0` definitionsgemäß falsch.
- **Impact eingegrenzt:** betrifft NUR die lokale QoS-Delivery-Metrik `PacketsUnrecoverableLoss`. Der RTCP-Wire-Report-Pfad (`InboundRtpStatistics.TrackSequence`/`CaptureRtcpReport`, `InboundRtpStatistics.cs:75-149`) ist separat und zählt jedes empfangene Paket → on-wire `CumulativePacketsLost` bleibt korrekt.
- **Präzise Fundstellen:** `EmitConcealmentFramesIfNeeded` @538–559 (Gap = `incomingSeq-(_lastDeliveredSequence+1)` @543f; `AddUnrecoverableLoss(gap-count)` @557f) · `MaxConcealmentBurstPackets`=3 (`const` @28) · `_lastDeliveredSequence` (`ushort` @70, vorgerückt nur bei Delivery @533 + Concealment @553) · `JitterBuffer` Late-Pfad @145–150 (return VOR Insert, kein Cursor) · Session-Handler `HandleJitterBufferAddResult` Late-Branch @660 (nur `RecordDroppedLate()`+Log — **hier fehlt das Cursor-Vorrücken**).
- **Fix-Präzisierung:** gehört in den Session-`Late`-Handler @660; `_lastDeliveredSequence` extended-sequence-aware auf `max(...)` vorrücken (analog telephone-event-Pfad @391f) UND Late gar nicht zu `UnrecoverableLoss` beitragen lassen. **Warnung: naives Vorrücken bei jedem Late maskiert echten Out-of-Order-Loss** → extended-seq-Vergleich zwingend.
- **Alternativen ausgeschlossen:** `PacketsDroppedOverflow=0` schließt Jitter-Buffer-Overflow aus; das Symptom skaliert mit dem `Late`-Count, nicht mit Socket-Loss.
